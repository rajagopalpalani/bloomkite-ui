import React from 'react';

// Simple client-side only lightbox component
const LightboxWrapper = (props) => {
    // Always return null - this component is for client-side only
    // The actual lightbox will be loaded dynamically on the client
    return null;
};

export default LightboxWrapper;
